{
  "name": "gauge-example",
  "version": "0.0.2",
  "description": "Gauge example",
  "devDependencies": {
    "gulp": "~3.8.10",
    "gulp-rename": "~1.2.0",
    "gulp-uglify": "~1.1.0",
    "gulp-jshint": "~1.9.0",
    "jshint-stylish": "~1.0.0"
  },
  "scripts": {
    "test": "gulp && git status | grep 'working directory clean' >/dev/null || (echo 'Please commit all changes generated by building'; exit 1)"
  }
}
